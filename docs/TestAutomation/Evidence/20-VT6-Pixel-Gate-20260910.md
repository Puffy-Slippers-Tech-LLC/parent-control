# Task 20 — VT6 prompt needle and exact pixel gate

## Result and reuse

The reviewed native 1024×768 credential-free prompt is now a maintained needle,
with an independently decoded exact pixel gate. The reference PNG is an
unchanged copy of the [retained prompt](20-VT6-Prompt-Qualification-20260910.md),
SHA-256 `63f310dff9a066270e8888af30ade4c65fc5e02af2604dc3bba53defff243f47`.
It was exported through the guarded helper and directly reviewed again before
use. It contains the canonical test-fixture/terminal labels, with no password.
The [owning contract](../../../tests/e2e/README.md#visible-vt6-installation-terminal)
records the exact rectangles, APIs, regression IDs and qualification limits.

Reused installed os-autoinst/tinycv matching, distribution staging and the
existing GDM matcher probe, now shared through `tests/support/needle_matcher.py`.
The existing GTK/GI GdkPixbuf decoder provides the missing strict comparison;
no new image library, setup change, renderer or discovery run was needed.

**New discriminating failure:** the initial full-frame-only needle returned
similarity 1 for single-glyph changes to the account, account suffix, challenge
input and VT label. The first selection failed four refusal cases (45 passed).
Tight selected-login/challenge and VT-label regions corrected these cases.
However, sparse output elsewhere still passes the matcher. The permanent
`test_needle_similarity_cannot_replace_exact_blank_screen_gate` counterexample
proves why the exact gate is mandatory alongside the needle.

`verify_prompt_pixels` rejects every decoded RGB difference outside the fixed
6×16 cursor cell, validates the reviewed reference digest and native dimensions,
bounds decoding, and returns only a fixed boolean proof or redacted refusal.
Staging permits only the fixed VT6 layout; generic password needles still
cannot acquire exclusions. No password helper accepts this new surface yet.

The slice ended at the screen boundary because real matcher behavior disproved
the proposed single-needle authorization assumption. This is local verification,
not a live authenticated qualification. Worker/controller wiring, fresh-capture
and selected-recipient continuity, one-shot input and session completion remain
unfinished. Identical stale pixels and invisible input cannot be distinguished
by a screen comparator; the cursor cell is deliberately unasserted.

## Verification and failures

- Final focused selection: `tools/run-unit-tests
  tests/unit/test_e2e_vt6_pixels.py tests/unit/test_e2e_gdm_pixels.py
  tests/unit/test_e2e_needle_inputs.py tests/unit/test_e2e_vt6_prompt.py
  -q --tb=short`: **112 passed**. Includes real matching, cursor blink,
  wrong account/suffix, absent or moved challenge, visible input/stale output,
  wrong VT/resolution, single-pixel differences, malformed/oversized/alpha images,
  reference changes, fixed error privacy, and immutable needle-layout guards.
- First `make check`: **exit 2**, **6,763 passed, 1 failed**. The pre-existing
  `test_rules_render_for_a_checkout_with_spaces` fixture omitted the now-required
  `publish-release` executable. Added that launcher to the simulated checkout;
  no rule, permission, production renderer or installed helper changed.
- `tools/run-unit-tests tests/unit/test_dev_tool_installation.py -q --tb=short`:
  **15 passed**. The [approval contract](../Approval-Tools.md#one-time-setup)
  now links this fixture requirement and regression.
- Final `make check`: **exit 0**, **6,764 unit/contract tests and 58 private-D-Bus
  component tests passed**, plus stage traceability and common source checks.
  No checkout edits were made during either common-check attempt.
- Documentation validation: **244 local links across 7 documents, none missing**;
  scoped `git diff --check` passed. Continuation retains exactly one supported
  settings line and selects the earliest ready task.
- No build, VM boot, credential provisioning or acceptance scenario was run.
  Historical prompt-route history remains 2 attempts (1 failure, 1 pass), and
  installation history remains 21 (3 historical passes, 18 failures).

## Cleanup and next action

All commands exited and results were collected. The sole caller-owned export,
`/tmp/onpc-vt6-needle-review.png`, was removed using `tools/cleanup-screenshots`;
the original private capture remains retained and the reviewed fixture is now
in the distribution. No VM lease, worker, callback, process recovery or pending
cleanup remains. No approval or Polkit denial occurred. **All-task VM clearance
persists**; implementation readiness, not the resolved hold, limits live input.

Task 20 remains earliest ready, no bypass; Task 15A's later handoff is preserved.
Task 20 acceptance, sudo/notice pixels, complete install/reboot and both E2E-028
startup faults remain unfinished.

Next: wire the needle **and** exact comparator into the one-shot VT6
worker/controller login flow. Use provenance-bound reference bytes and a fresh
private capture, bracket it with unchanged-boot and selected recipient checks,
seal captures before secret access, latch failures, and require the VT6 session
observation plus shell/recipient continuity. Then run isolated safety checks and
the smallest guarded authenticated success/refusal qualification. Do not repeat
credential-free prompt collection.

Actual settings: `gpt-6-astra` / `high`, Standard. Next settings:
`gpt-6-astra` / `high`, Standard: screen matching is locally proven, but the
authorization and one-shot input continuity across worker/controller remain
unresolved security/correctness boundaries.
