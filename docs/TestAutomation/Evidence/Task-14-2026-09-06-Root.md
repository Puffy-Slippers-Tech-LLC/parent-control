# Task 14: root method permissions passed — 2026-09-06

Scope: development host and the existing guarded `ubuntu26.04` VM. This slice
implemented and verified root's installed broker method permissions. Task 14
remains incomplete; this selected result does not establish full-area or release
acceptance. Prior working-tree changes and evidence were preserved.

## Implemented and observed

- `system_caller.py` accepts UID 0 only with literal `allow_root: true` in the
  guarded caller protocol. It sets and verifies real/effective/saved UID/GID,
  resets supplementary groups and environment, and verifies the actual bus UID.
  Ordinary callers and authentication agents still reject root by default;
  the agent interface cannot opt in. Seven additional unit cases cover opt-in,
  credential verification and agent refusal.
- `test_root_method_permissions_and_approver_exclusion` shares the existing
  17-method inventory and `call`/`batch` transport. It proves 12 allowed cells
  and five denied request/own-account cells with actual UID 0 connections.
  `SetPreferences` and `SetParentControl` produce observable saved changes.
  Every method preserves the other six fixture accounts' preference, limit,
  grant and filter state; denials also preserve the target's state.
- Root is absent from managed-child and approver discovery, while both eligible
  children/parents remain present. Selecting root as approver fails on both
  enabled-child and kiosk request paths without account-state writes. Root may
  log to the parent component, but child/kiosk/broker component writes fail.
- The selected case restores its target settings through broker calls. The
  runner separately restores the retained baseline and domain configuration.
  No product or host-setup changes were needed.

## Verification and inputs

The isolated runner/caller/agent cleanup prerequisites passed **30 tests**.
Focused caller regressions passed **25 tests**. `make check` passed **1,210
unit/contracts and 17 private-D-Bus component tests**, plus its syntax, source
and stage-mode traceability checks (session 43777, exit 0). `git diff --check`
passed. No guest pytest was invoked on the host.

Host-safe collection resolved exactly five executions. One fresh build and one
guarded run completed:

```sh
make build-test-artifacts OUTPUT_DIR=/tmp/onpc-task14-20260906-root-permissions
make check-system LIST=1 AREA=authorization TEST=test_root_method_permissions_and_approver_exclusion
pkexec make -C /Data/Code/PST/parent-control check-system ARTIFACT_DIR=/tmp/onpc-task14-20260906-root-permissions AREA=authorization TEST=test_root_method_permissions_and_approver_exclusion
```

Build session 17587 exited 0 after artifact verification. Installed-run session
79852 exited 0. Evidence: `/tmp/onpc-system-o0xgf72y/evidence`.
All **five executions passed**: the root case and four install/reboot
prerequisites, with no failed, skipped, missing, additional or duplicate cases.
Product, infrastructure, collection and cleanup each passed.

Revision: `70e16c080564d8b83cc4b8d605fece01d1028290`, plus captured working tree
(346 source files). Exact SHA-256 identities:

| Input/evidence | SHA-256 |
| --- | --- |
| Source | `0a54bc83a5aba226ee08aa945ae1c7d97d38228f0a586aa8ba005049cbf87c70` |
| Package | `11fe09f10b3921ca6d5718a8311ddd5d233e7429bdc76865d2456289a18896b3` |
| Stable fixtures | `1a2e4e731d822cb31cf901ffcbbec617f588701b40edd9e65ccf486ad6379d43` |
| Selected inputs | `90a229849ea4dc50e6a24af7b5f6d6c08b5564e948f0c15e997beec44dd09040` |
| Authorization XML | `199ec374e1928ad90ffdf829f159377c61a1a92206f456c7e63fb5e6ccf307b5` |
| Aggregate result | `8ec0258c9bbb39a07f13174a008eecc537406eb75e09afcf58f1901153632417` |

The existing read-only `/tmp/onpc-task14-kiosk-revalidation-audit.py` reconciled
expected, reported and actual JUnit identities, uniqueness, successful outcomes
and completed cleanup for this single-case selection. No authentication challenge
was expected or recorded. Redacted broker logs corroborate management acceptance,
request denials, revocation and component restrictions; the parent log contains
the accepted matrix message. All caller/system-test edits were exercised in the VM;
subsequent edits document the result and reusable contract only.

## Cost and handoff

| Stage | Seconds |
| --- | --- |
| Preparation | 72.049 |
| Bootstrap | 44.564 |
| Install | 48.563 |
| Reboot | 19.625 |
| Tests | 44.526 |
| Collection | 1.807 |
| Cleanup | 101.955 |

One installed attempt took approximately 5.6 minutes of runner stages; build,
host authentication, implementation and audit time are additional. Baseline
preparation/restoration still dominate the VM cost. No retry was needed. No
context/usage telemetry was available. This batch covers all root cells in one
VM cycle. The prior authorization matrix was not repeated; its
[225-execution evidence](Task-14-2026-09-06-Authorization.md) remains valid for
those inputs. Final Task 14 acceptance still requires the completed area.

All owned commands finished. Cleanup verified restored baseline/domain and host
preservation; the final read-only domain check returned `shut off`. No operation
is pending. Next implement stale/deleted identity assertions, beginning with
actual target/approver deletion and fresh discovery/request results. See the
[active handoff](../Task-14.md#continuation-handoff--2026-09-06-incomplete).
