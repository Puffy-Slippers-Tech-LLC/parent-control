"""Implementation modules for the single `make publish` entry point.

Source verification and clean builds are libraries. signing.py is the GPG
adapter invoked by Git/debsign, not a separate release workflow.
"""
