# Development host: libvirt graphics FD exchange from the classic VS Code snap.
# Only anonymous Unix stream sockets; no named sockets, ptrace, or QEMU rule.
unix (send, receive) type=stream addr=none peer=(label=snap.code.code addr=none),
