{
  "type": "object",
  "properties": {
    "status": {"type": "string", "enum": ["ready_for_vm", "task_complete", "blocked"]},
    "task_id": {"type": "string"},
    "summary": {"type": "string"},
    "handoff": {"type": "string"},
    "host_validated": {"type": "boolean"},
    "live_result": {"type": "string", "enum": ["not_run", "failed", "passed"]},
    "stage_paths": {"type": "array", "items": {"type": "string"}}
  },
  "required": ["status", "task_id", "summary", "handoff", "host_validated", "live_result", "stage_paths"],
  "additionalProperties": false
}
