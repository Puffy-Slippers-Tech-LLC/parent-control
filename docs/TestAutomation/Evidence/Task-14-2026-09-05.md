# Task 14 recorded evidence — 2026-09-05

Historical evidence for the recorded inputs. Read only when needed from the
[active handoff](../Task-14.md#continuation-handoff--2026-09-05-incomplete).
Model selection, task order, experiment limits and future commands are owned
by the [current workflow](../Implementation-Workflow.md) and [backlog](../Test-Automation.md).
The verification/next-run text below describes the pre-F1 runner; it does not
authorize repeated full runs or bypass new prerequisites. Machine state is a
recorded observation, not a guarantee about another session.

### Interfaces and changes to preserve

Reuse [installed runner contracts](../../../tests/integration/README.md#reusable-implementation-contracts),
`FixturePassword`, `PersistentCaller`, and `TextAgent`. Registration uses
`pkttyagent --process PID,START_TIME`, a private controlling PTY and notification
pipe. Prompt readiness verifies selected identity and echo-off state; outcome
handling consumes the first complete accepted/denied/cancelled marker. Cleanup
signals only directly spawned pidfd-pinned processes. Credentials stay in memory
and are installed through stdin without diagnostic export.

This continuation changes `system_caller.py`, `test_system_agent.py`, this
handoff and the reusable runner guide. New regressions distinguish PAM auth,
PAM account, authority-response and unknown diagnostics while proving no arbitrary
terminal text reaches output/errors. Sources consulted:
[Polkit PAM helper](https://github.com/polkit-org/polkit/blob/master/src/polkitagent/polkitagenthelper-pam.c)
and [authority response helper](https://github.com/polkit-org/polkit/blob/master/src/polkitagent/polkitagenthelperprivate.c).
These explain diagnostic markers, not the installed failure's root cause.

Activation `none`: no packaged integration, migration or host dependency change;
no product installation on this host. Preserve unrelated concurrent work,
including `tools/publish_release.py` and its tests. No commit made.
`system_runner.PHASE_COUNTS['authorization']` remains 221.

### Verification and next run

- Run isolated prerequisites before integrated process cleanup:
  `/usr/bin/python3 -B -m pytest tests/unit/test_system_agent_cleanup_safety.py tests/unit/test_system_caller_cleanup_safety.py tests/unit/test_system_runner_cleanup_safety.py tests/unit/test_prepare_host_cleanup_safety.py tests/unit/test_child_preview_cleanup_safety.py -q`.
  This continuation: **43 passed, three subtests passed**.
- After the helper diagnostic change, `make check`: **854 unit/contract and
  17 component tests passed**, including stage traceability and seven release
  tests added concurrently outside this task.
- `git diff --check` passed after handoff edits.
- Latest build: `/tmp/onpc-task14-20260906-auth-diagnosis`.
  Package SHA-256:
  `fbb09d829386aeb7eb9f30ccdb1a1ee8e43f68de9dd0188c06740a4d33b8ef41`.
  Revision `087e65579f7c4745546b4cb92cbc6c56e6cdd1da`;
  source digest `28b8205a527dc6467dadecdd414fa462870718fcb186938dc7f8b0c4fa3ceea7`.
  The builder digest includes all tracked/untracked source inputs, tests and
  docs. New diagnostic code and this handoff postdate that build/frozen VM input.
- Build fresh current inputs using
  `make build-test-artifacts OUTPUT_DIR=/tmp/<new-empty-directory>`, then
  `pkexec make -C /Data/Code/PST/parent-control check-system ARTIFACT_DIR=<verified-directory>`
  with platform sandbox escalation. Allow roughly seven to nine minutes for
  the guarded attempt/cleanup; never invoke guest pytest on this host or bypass
  guards to select cases.

### Evidence and machine state

Latest failed attempt: `/tmp/onpc-system-kpjcrm54/evidence/`.
`guest/authorization.xml`: **213 passed, eight failed**, no errors/skips,
94.800 seconds. All failures remain `agent:unexpected-denied` in the cases above.
Install/reboot assertions passed. Authentication journal collection succeeded;
its evidence is described above. `result.json`: `outcome=failed`,
`category=command:failed:ssh`, `cleanup_phase=complete` (guest pytest failure,
not an SSH connectivity failure).

The original runner survived the user interruption and was resumed, not
restarted. It completed baseline/domain restoration and host preservation
checks. Independent `virsh --connect qemu:///system domstate ubuntu26.04`
returned `shut off`. All run/read commands exited; no recovery or process
resumption is pending.

Preserve earlier failed evidence at `/tmp/onpc-system-lc5oclev/evidence/`,
`/tmp/onpc-system-8g4zlvv1/evidence/`, `/tmp/onpc-system-z5avn5xw/evidence/`,
and `/tmp/onpc-system-4blqk7sg/evidence/`, plus passing 213-case evidence at
`/tmp/onpc-system-7utjj2ct/evidence/` and `/tmp/onpc-system-61fofrz8/evidence/`.
Later success never relabels failures. The old empty authentication journal in
`lc5oclev` was a collection error, not proof that PAM logged nothing.

