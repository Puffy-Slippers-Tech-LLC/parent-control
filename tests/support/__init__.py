"""Opt-in test support, independent of collected test-case modules.

Import the specific helper or fixture a suite needs. Nothing here starts a
process, changes host services, or acquires a VM lease at import time.
"""
