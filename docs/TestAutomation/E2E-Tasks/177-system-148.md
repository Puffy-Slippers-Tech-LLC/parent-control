# 177 — Preserve and qualify E2E-029/concurrent-transaction

Budget: 30–60 minutes, including a normal verification cycle; this is not a stop timer.
Follow the [master session contract](../E2E-Execution-Plan.md#execute-one-task) and repository approvals.

## Scope and entry

Preserve numeric case **148**, E2E-029/`concurrent-transaction`, and all its [inventory obligations](../../../tests/e2e/scenarios.json). This is mechanical system qualification, not customer UI coverage. Read [inventory reconciliation](../E2E-Building-Blocks.md#inventory-reconciliation) and the [installed system runner](../../../tests/integration/README.md#running-the-current-installed-suite).

## Work

Exercise the declared concurrent real transactions and prove serialization, time/policy correctness and other-user isolation; remove the conflict and perform the declared successful recovery. Do not infer serialization from one visible prompt.

Map each original step/assertion to its existing unit/component/system owner. Reuse completed tests unchanged. Add only the missing bounded system case/control if needed, with ownership/cleanup safety regressions; no customer fault block or replacement harness. Keep the original ID traceable if dispatch ownership changes. A transfer alone earns no passing coverage. If a missing mechanism cannot fit a focused session, split its implementation before the live consumer in the master; do not bundle a new fault framework here.

## Live VM acceptance

Inspect `tools/run-tests system --list --area 'authorization'` and select the exact maintained cases covering this fault, failure-before-recovery, real recovery and isolation. Build/reuse fresh verified artifacts through `tools/run-tests artifacts build`. Run the scoped system selection with its returned artifact directory:

```sh
tools/run-tests system --artifacts '<verified-directory>' --area 'authorization' --test 'test_e2e_concurrent_transaction_recovery'
```

The test name is a proposed fallback when no existing exact case covers the obligation; replace it with the listed maintained selector when reusing one, and run every required selector. Do not run the placeholder path literally. Complete only after the full fault/recovery obligation passes in the guarded live VM and owned cleanup succeeds. A partial mapping or only host tests leaves the missing scope pending.

## Close out

After successful cleanup, check this task in the [master](../E2E-Execution-Plan.md) and update [catalogue/scenario status](../E2E-Building-Blocks.md) using its readiness rules. Delete this task when no longer needed, replacing its master link with plain text. No new evidence/history document. Record current system owner/selectors in the guide's reconciliation table, retaining the original numeric ID. Never mark this a customer scenario pass.
