[config]
# Pytest captures console diagnostics and retains per-test application logs.
# Dogtail's default shared /tmp file is truncated by each importing process.
# Disable that file through its supported configuration before any UI imports.
debug_file = None
debug_file_persistence = True
