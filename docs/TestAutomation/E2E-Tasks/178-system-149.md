# 178 — Preserve and qualify system obligation 149: policy reload

Estimate: 30–60 minutes. Aim for one session; this is not a stop timer.
Follow the [session contract](../E2E-Execution-Plan.md#task-size-and-order).

Session exception: The exact system fault, safe failure, real recovery, unrelated-user isolation and cleanup must remain one complete live obligation under its maintained owner.

## Read only this context

Use the [scoped reading rules](../E2E-Execution-Plan.md#load-only-the-selected-context).
Load only this numbered engineering obligation, its reconciliation entry and
relevant maintained system-test callables. Do not load other task briefs,
customer families or the full queue/inventory.

## Scope and implementation

Required tasks: none (Baseline).

Preserve engineering case **149** and all its original
[retained engineering obligations](../E2E-Building-Blocks.md#inventory-reconciliation).
Use the existing mechanical owner from
[inventory reconciliation](../E2E-Building-Blocks.md#inventory-reconciliation).
This is system qualification, with no customer UI acceptance credit.

Cause the declared policy-reload failure, prove prior rules are restored or remain fail-closed without partial success, then restore the cause and retry. Preserve actual enforcement and unrelated-user assertions.

Reuse existing tests and guarded controls. Add only the missing bounded case
with ownership/cleanup regressions. If a missing mechanism needs a separate
session, split it ahead of this consumer in the master; do not build a general
fault framework. Preserve failure-before-recovery and unrelated-user isolation.

## Live VM acceptance

List the maintained selectors:

```sh
tools/run-tests system --list --area 'enforcement'
```

Select every exact case covering this obligation. Build/reuse verified artifacts
through `tools/run-tests artifacts build`, then run the listed selections:

```sh
tools/run-tests system --artifacts '<verified-directory>' --area 'enforcement' --test '<listed-test>'
```

Replace both placeholders with actual returned values. If no existing test
covers the obligation, implement a bounded `test_e2e_policy_reload_recovery` under the same owner and
validate that it is listed before running it. Require the complete fault,
observed failure, real recovery, isolation and cleanup on the guarded live VM.
A mapping, proposed name or host test alone leaves the obligation pending.

## Close out

After acceptance and cleanup, retain the current owner/selector and qualification
status in [E2E-Building-Blocks.md](../E2E-Building-Blocks.md#inventory-reconciliation)
and the engineering scope of
[E2E-Scenario-Recipes.md](../E2E-Scenario-Recipes.md#coverage-ownership-and-remaining-limits).
Preserve numeric case 149; never count it as a customer pass. Regenerate coverage
after inventory/collection changes with `tools/generate_test_coverage.sh`, which
runs `tools/generate_test_coverage.py`.

Check this row in the [master's queue](../E2E-Task-Queue.md), update the
master's **Next task** pointer, then delete this brief
when no longer needed and replace its queue link with plain text. Validate changed
Markdown with `tools/read-only links`. Keep normal artifacts; no new history report.
