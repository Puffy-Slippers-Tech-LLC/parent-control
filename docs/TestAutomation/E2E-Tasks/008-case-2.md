# 008 — E2E-002: clean

Estimate: 40–60 minutes. Aim for one session; this is not a stop timer.
Follow the [session contract](../E2E-Execution-Plan.md#task-size-and-order).

Session exception: Real installation, reboot, default-state inspection and station entry stay in one customer journey.

## Read only this context

Use the [scoped reading rules](../E2E-Execution-Plan.md#load-only-the-selected-context).
Read only the named block rows/callables, this recipe's selected cases and
applicable finite-data rows. Prerequisite IDs are completion checks; do not open
their task briefs. Do not load the full queue, catalogue, recipe book or inventory.

For the app-defaults portion, use the qualified
[app-row contract](../E2E-Building-Blocks.md#app-row-observations):
`AccessibleUI.app_rows` / `app_row_operation` in `tests/e2e/accessible_ui.py`,
`AppRowsObservation` in `tests/e2e/ui_observations.py`, and the caller-owned
Allowed/default comparisons in `AppRowJourney.check_settings` in
`tests/e2e/app_row_observations.py`. The existing slice uses
`tests/integration/graphical_smoke/lib/onpc_app_rows.pm`; compose its public
operations into the complete case rather than invoking a separate qualification
or restoring another snapshot inside the customer journey. Host regression
selectors are `tests/unit/test_e2e_app_rows.py` and
`tests/unit/test_installed_journey_cleanup_safety.py`.

## Scope and prerequisites

Implement only these [inventory](../../../tests/e2e/scenarios.json) bindings:

- **2 — clean:** `installation=clean`.

Use the [E2E-002 recipe](../E2E-Scenario-Recipes.md#e2e-002).
Apply its explicit entry/transition rules; do not copy or replace its data here.
Each case starts a fresh guarded product-free package-lifecycle attempt.

Required tasks (queue IDs; use delivered scope, not predecessor briefs):

- **007** — LIFE02.
- **013** — REQUEST11/12 kiosk Cancel and Escape.
- **077a** — PARENT12; UI13 complete public app-row observations.

## Implementation and expected results

Require the exact final reboot notice, a real normal reboot, preserved personal-account choices and the offered station. Read Parent's fresh limits-off/zero/unblocked defaults, then enter and cancel the station. Installation occurs inside this continuous customer journey.

Compose and register the complete worker/JourneyPlan through the
[existing consumer path](../E2E-Building-Blocks.md#add-a-consumer), with unique
stages, explicit observations and deadlines. Preserve all selected branches and
separate engineering obligations.

## Live VM acceptance

Finish functional edits and affected safety/adapter checks before the live run.
Execute the selected case independently. After its complete journey, collection
and cleanup pass, refresh coverage before advancing the task:

```sh
tools/run-tests e2e --id '2'
tools/generate_test_coverage.sh
```

The launcher runs `tools/generate_test_coverage.py`. Require this
case's full public results and terminal cleanup; generation alone is not a pass.
Preserve failures, staged inputs, evidence and VM ownership through cleanup.
Concurrent checkout edits follow the documentation map and do not invalidate the run.

## Close out

After this case's complete live acceptance and cleanup, follow the
[master completion contract](../E2E-Execution-Plan.md#completion-and-document-cleanup).
Refresh coverage after this case succeeds.
Use `tools/generate_test_coverage.sh`, which runs `tools/generate_test_coverage.py`.

Update the relevant callable/scope/status in
[E2E-Building-Blocks.md](../E2E-Building-Blocks.md) and update the selected recipe only when
its composition changes. Runtime status belongs in the inventory; leave
unfinished scope pending.
Check **008** in the [master's queue](../E2E-Task-Queue.md), update the master's
**Next task** pointer, then delete this brief once its enduring context is maintained
in source/contracts. Validate changed Markdown. Keep normal runner artifacts;
no task archive, evidence document or accumulated history.
