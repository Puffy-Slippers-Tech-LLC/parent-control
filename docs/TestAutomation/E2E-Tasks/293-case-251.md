# 293 — E2E-051: jordan

Estimate: 30–50 minutes of implementation plus the complete continuous VM run;
continue as needed and preserve the recipe's run bound. Follow the [master session contract](../E2E-Execution-Plan.md#execute-one-task).

## Read only this context

Use the [scoped reading rules](../E2E-Execution-Plan.md#load-only-the-selected-context).
Read only the named block rows/callables, this recipe's selected cases and
applicable finite-data rows. Prerequisite IDs are completion checks; do not open
their task briefs. Do not load the full queue, catalogue, recipe book or inventory.

## Scope and prerequisites

Implement only these [inventory](../../../tests/e2e/scenarios.json) bindings:

- **251 — jordan:** `first-child=jordan`.

Use the [E2E-051 recipe](../E2E-Scenario-Recipes.md#e2e-051).
Apply its explicit entry/transition rules; do not copy or replace its data here.
Each case starts a fresh guarded installed-app attempt.

**Gate:** Require the declared real-game/editor assets and public results, enough time before midnight, and every cycle/round in one attempt within 5400 seconds.

Required implemented capabilities (IDs identify queue rows; no predecessor brief is needed):

- **197** — FLOW20 overlay new/open form.
- **197k** — FLOW20 kiosk new/open form and fresh/retained child.
- **198** — FLOW15/FLOW01 other-parent management entry.
- **196** — FILE09.
- **079b** — FLOW19.
- **079a** — APP02 and FLOW08 native grid/command policy results.
- **155** — FLOW12 current choices.
- **126** — APP05/FLOW10 windowed game.
- **047a** — FLOW09 and FLOW14 distinct-user retention.

## Implementation and expected results

Complete four rounds and both child visits per round in one attempt. Alternate the parents' own management windows, read the peer before edits and compare independent work/choices with elapsed-time margins. Finally revoke only the grant-only child and prove the daily-only peer remains usable.

Compose and register the complete worker/JourneyPlan through the
[existing consumer path](../E2E-Building-Blocks.md#add-a-consumer), with unique
stages, explicit observations and deadlines. Preserve all selected branches and
separate engineering obligations.

## Live VM acceptance

Finish functional edits and affected safety/adapter checks before the live run.
Execute each selector independently. Only after its complete journey, collection
and cleanup pass, run the following coverage command before any next case:

```sh
tools/run-tests e2e --id '251'
tools/generate_test_coverage.sh
```

The launcher runs `tools/generate_test_coverage.py`. Require every selected
case's full public results and terminal cleanup; generation alone is not a pass.
Keep source/documents unchanged through cleanup and preserve failures.

## Close out

After every selected case's complete live acceptance and cleanup, follow the
[master completion contract](../E2E-Execution-Plan.md#completion-and-document-cleanup).
Refresh coverage after each successful case, including each case in a pair.
Use `tools/generate_test_coverage.sh`, which runs `tools/generate_test_coverage.py`.

Update the relevant callable/scope/status in
[E2E-Building-Blocks.md](../E2E-Building-Blocks.md) and the selected family's status
in [E2E-Scenario-Recipes.md](../E2E-Scenario-Recipes.md); leave unfinished scope pending.
Check **293** in the [master's queue](../E2E-Task-Queue.md), update the master's
**Next task** pointer, then delete this brief once its enduring context is maintained
in source/contracts. Validate changed Markdown. Keep normal runner artifacts;
no task archive, evidence document or accumulated history.
