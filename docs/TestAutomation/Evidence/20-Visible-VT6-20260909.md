# Task 20 — visible VT6 surface and fixed recipient probes

## Result and scope

The accepted baseline supplies a real VT6 login terminal, rendered by the guest
kernel and visible over VNC. This resolves the launch-surface uncertainty in
the [earlier boundary finding](20-Graphical-Notice-Boundary-20260909.md).
The [owning contract](../../../tests/e2e/README.md#visible-vt6-installation-terminal)
records the selected route, canonical probes, regressions and remaining limits.
Actual settings: `gpt-6-astra` / `high`, Standard.

One guarded maintenance inspection used `tools/test-vm start`,
`tools/test-vm send-key 29 56 64` (Ctrl+Alt+F6), `tools/test-vm screenshot` and
`tools/test-vm stop`. All exited zero. No login, password, package installation,
guest setup edit or customer reboot was performed. This is one discriminating
maintenance experiment, not another installation qualification attempt.

The retained private original is `/tmp/onpc-vm-screen-ixrcta61/screen.png`.
The approved artifact reader produced the reviewed copy
`/tmp/onpc-artifact-export-mv_pwsgp/screen.png`; it is retained as evidence.
SHA-256: `98c13f978cbfd40f25e73b1557b2c2547c788239f4236c872e194a0c35c1f14c`.
Direct visual review found a 1280×800 black kernel terminal, an Ubuntu 26.04.1
banner identifying tty6, and its empty login prompt. No credential or account
selection appears. The original and copy remain private; neither is an approved
authentication needle or public full-screen attachment.

## Implementation and verification

Added fixed serial/VT6 contexts in `tests/e2e/terminal_observations.py` and reused
the existing login, sudo installation and sudo reboot proofs for VT6. The new
read-only observer names bind to exact getty unit/device and repeatedly check
the active VT. The transport distinguishes surface/purpose tokens and latches
failures. No worker password input or capture policy changed.

- Isolated `test_vm_control_cleanup_safety.py`: **16 passed** before VM access.
  Each guarded mutation also ran its mandatory prerequisite selection:
  **580 passed, 3 subtests passed** for start, key input and stop.
- Initial focused probe run: **12 failed, 2,299 passed**. All failures were
  VT6 variants of
  `test_install_password_requires_exact_sudo_fixture_and_no_echo`, for the
  initial/recipient/continuity executable-resolution diagnostic. The reused
  diagnostic still queried the serial getty and returned `leader-unavailable`.
  The input proof remained refused. Fixed the diagnostic to use its selected
  terminal unit, and expanded its direct regression over both units.
- Corrected selection: **2,349 passed**, covering
  `test_e2e_serial_observation.py`, `test_e2e_install_password_observation.py`
  and `test_e2e_observation_transport.py`. Reproduction uses
  `tools/run-unit-tests` with those three explicit `tests/unit/` paths and
  `-q --tb=short`. Existing wrong-recipient, command, echo and continuity cases
  now run for both terminals; new cases reject initial/late foreground loss,
  active-VT read errors, other-surface/purpose tokens and private output.
- Compatibility selection: **434 passed** across `test_e2e_install_helper.py`,
  `test_e2e_serial_helper.py`, `test_e2e_installation_boundary.py`,
  `test_graphical_smoke.py` and `test_e2e_needle_inputs.py`, through the same
  validated unit launcher. No full common/package/E2E acceptance was claimed.
- Documentation validation: **182 local links checked, none missing** across
  the owning contract, reuse map, handoff, continuation and this evidence;
  scoped whitespace checks passed.

## Cleanup and next boundary

The original stop command remained attached through `cleanup-requested`,
`restored-baseline-verification` and `complete`, then exited zero. The guarded
owner restored and verified the retained baseline and original VM configuration,
leaving the VM off. No source edit occurred during maintenance; source work
began after the stop completed. All launched commands have exited. The two
private screenshots are intentionally retained; no process, lease or recovery
obligation remains. No approval denial occurred. All-task VM clearance persists.

The VT6 probes are locally tested, not live-qualified. Still needed: a reviewed
terminal login/fixture-echo and sudo challenge contract, VNC worker/controller
wiring with session/boot checks, no-retry qualification, real final red-notice
pixels, and complete installation/reboot/startup acceptance. Keep installation
attempt history unchanged at 21 (3 historical passes, 18 failures).
Next settings: `gpt-6-astra` / `high`, Standard, for the first authentication
and private pixel-evidence boundary on the now-proven surface.
