#!/usr/bin/python3 -IB
"""Run the reconnectable test/repair loop; use --help for options."""
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent))
from fix_tests import main

if __name__ == '__main__':
    sys.exit(main())
