# Task 19A inventory slice — 2026-09-07

Completed the scenario inventory/selection boundary on the development host.
Task 19A remains unfinished; no graphical scenario was run or accepted.

## Implemented inputs

Base checkout: `ed269eff759db38171d9ac9a60eda1d98d659f2b`; changes were uncommitted
when verified. No product files, host dependencies, installed rules or VM state
were changed by this slice.

| File | SHA-256 of verified code/data |
| --- | --- |
| `tests/e2e/inventory.py` | `1ba73a285cabb96e645bdc2a25b6e99b69946e708fbccb7c460e423f67d4bc44` |
| `tests/e2e/scenarios.json` | `c2d2bac4bea5da49be82f41a7de3e8f9e2780fbbee9c2bad09450dda647e35d4` |
| `tests/unit/test_e2e_inventory.py` | `eb73d4b138d3740f93a69d1ba1218b32c975d6b4c2b7cbcdd21a12f72d0007f6` |

The inventory contains 33 families and 156 explicit pending variants. It
includes all 29 required starting families plus About/license, feedback
draft/review, authorized real delivery and declared transport-fault retry.
Unmapped installation/About/feedback obligations retain explicit owners and
requirement gaps. Nothing is marked runtime-covered.

Implemented schema/version checks, exact family/variant selection, preservation
and runnable refusal of pending cases, dimension/combination closure, canonical
owners/executable identities, executable path containment, separate attempt
phases, intervention declarations, three-sided assertions and minimum evidence
field declarations. CLI inspection is read-only, works outside the checkout
directory and never loads a worker or guest fixture. See the
[reusable contract](../../../tests/e2e/README.md).

## Verification and experiments

| Scope | Result |
| --- | --- |
| Focused inventory regressions, final inputs | `/usr/bin/python3 -B -m pytest tests/unit/test_e2e_inventory.py -q`: 102 passed in 0.41 s. |
| Initial cleanup prerequisites inside sandbox | 10 failed, 165 passed, 3 subtests passed; socket descriptor wrapping raised `PermissionError: Operation not permitted`. No live VM operation ran. |
| Same isolated cleanup scope outside sandbox, explicit filenames | 175 passed, 3 subtests passed in 1.83 s. Original command session 54046 exited 0. |
| `make check`, final code/data inputs | 1,395 unit/contracts passed in 17.41 s; 17 private-D-Bus components passed in 0.44 s; stage traceability, C/JS/Python/XML and source checks passed. Command session 27626 exited 0. |
| Whitespace validation | `git diff --check` passed. |

The isolated prerequisite scope was every current
`tests/unit/test_*cleanup_safety.py` module plus `test_graphical_lease.py`, expanded
to 13 explicit filenames before invocation. The first escalation used a shell
wildcard, which prevented matching the already-loaded pytest prefix approval;
the user interrupted that approval request. The explicit-filename command then
ran under the existing approval. No additional or broader rule was added.
This was an execution-policy command-shape correction, not a product fix.

The first focused iteration passed 101 tests; the final additional regression
checks real delivery retry classification and required authorization/profile
declarations. No expensive VM attempt, artifact build or compatibility rerun
was needed. Work/verification occupied roughly 16 minutes within a roughly
30-minute wall interval, which included the interrupted approval wait. VM
preparation/test/cleanup time was zero. Context/token telemetry was unavailable.

## Remaining boundary

The evidence contract currently validates declarations, not runtime records or
copied artifacts. Implement the runtime validator/private collector next, then
connect it to the qualified lease/worker in the guarded launcher. Registration
does not prove a safe helper, actual scheduled test ID, complete journey or
passing runtime assertion. All customer scenarios remain pending for their
owners. No owned command or VM operation remains active; this slice did not
query or change the VM, so the previous shut-off observation is historical.
