# 297 — E2E-052: appimagelauncher-login-autostart

Estimate: 20–30 minutes of composition + up to 30 minutes live. Aim for one session; this is not a stop timer.
Follow the [session contract](../E2E-Execution-Plan.md#task-size-and-order).

Session exception: Case 253 keeps all five stages and its complete 30-minute bounded live journey in one attempt after composition.

Required tasks (queue IDs; use delivered scope, not predecessor briefs):

- **296b** — APP06/UI22 continuous login interval and Lunar autostart binding.
- **180** — FLOW01 same-user entry; FLOW16 fresh/same Parent allowance setup.
- **197** — FLOW20 overlay new/open form.

Use the [master](../E2E-Execution-Plan.md#execute-one-task), the
[complete case 253 recipe](../E2E-Scenario-Recipes.md#e2e-052) and only its named
block rows. Manual prerequisites follow the
[profile contract](../E2E-Building-Blocks.md#lunar-client-preparation-and-observation-gate).

## Implementation and acceptance

Bind the full five-step recipe in the established inventory/worker path without
new infrastructure or product probes. Preserve the initial working autostart and
Minecraft controls, zero-grant denial, active time-only reboot denial, explicit
soft inclusion and replacement-exclusion reboot. Require every intermediate
public result and time margin; do not repair policy after boot or bypass the
AppImageLauncher route. Assets/providers unavailable means pending, not skipped
acceptance. Do not modify the product's active-grant session contract.

After prerequisites and affected safety checks pass, implement the executable
binding and run the complete case through `tools/run-tests e2e --id '253'` on
the guarded observed VM. This selector is intentionally non-runnable now. Require
private evidence, continuity, reconciliation and successful cleanup; a host unit
test or metadata listing is not an installed pass.

After the complete case passes, run `tools/generate_test_coverage.sh` and follow
[close-out](../E2E-Execution-Plan.md#completion-and-document-cleanup). Until then
keep the row unchecked and scenario pending, retaining exact remaining blockers.
