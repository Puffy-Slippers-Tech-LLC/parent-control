# Aggregate failure investigation — 2026-09-12, 20:27 run

Original [report](test-all-runs/20260912T202723Z-981484cc/report.md) and
[category results](test-all-runs/20260912T202723Z-981484cc/progress.json) remain
unchanged. Unrelated staged work is preserved.

## Findings and fixes

The two unit failures in `test_installer.py` expected `apt install <deb>`.
The existing Makefile and [installation contract](../../Publishing.md#package-update-activation)
require `apt install --reinstall <deb>` to install rebuilt same-version payloads.
Corrected both assertions; the existing executable test still verifies APT
arguments, delegation, output and failure propagation. The clean publishing
build failed on exactly these same two assertions, confirmed in
`/tmp/onpc-ppa-check-uthctxa0/output/oh-no-parent-control_1.2+ppa1~ubuntu26.04.1_amd64-2026-09-12T20:43:57Z.build`.

The E2E attempt `scenario-af60bd55ee3c47d3a74094e39d68dd23` refused
`provenance:source-changed` during `VerifiedInputs` construction. Preflight
passed; no product assertions ran; owned cleanup passed. Retained details are
in `/tmp/onpc-e2e-evidence-igdx3zt8/invocation-000003.json` and
`/tmp/onpc-graphical-smoke-md696ffq`. The operator reports no intentional edits.
The retained evidence contains no differing snapshot, so the changed file,
field and writer remain unknown. No source exclusion or relaxed comparison is
justified. Added fixed change counts to the existing rejection diagnostic;
the [owning contract](../../../tests/e2e/README.md#controller-owned-provenance)
defines its privacy and refusal behavior.

## Verification and remaining work

- Installer tests: 14 passed.
- Isolated cleanup prerequisites: 806 passed, 3 subtests passed.
- `tools/run-tests publish`: passed, including source checks, clean sbuild and
  Lintian. Evidence: `/tmp/onpc-test-publish-loemwrkl`, clean build
  `/tmp/onpc-ppa-check-v01fok34`. Its frozen inputs include the installer fix
  and predate the diagnostic additions. Clean build: 8,058 unit tests and
  134 private-D-Bus components passed.
- Initial `make check`: passed, 8,058 unit tests and 134 components. This began
  before the new diagnostic tests and the concurrent launcher changes below.
- `tools/run-unit-tests 'tests/unit/test_e2e_provenance.py'
  'tests/unit/test_e2e_execution_cleanup_safety.py' -q`: 109 passed, including
  all five new diagnostic cases. Diagnostic behavior is locally tested;
  live source-change counts are not yet qualified.
- `tools/run-tests artifacts build`: passed; output
  `/tmp/onpc-test-artifacts-fl1l1jop`. The E2E launcher repeated isolated safety
  prerequisites (806 passed, 3 subtests) before the selected rerun.
- `tools/run-tests e2e --artifacts /tmp/onpc-test-artifacts-fl1l1jop
  --scenario E2E-001`: refused `provenance:package-source-mismatch` before lease
  acquisition or VM mutation. Evidence: `/tmp/onpc-e2e-evidence-itfy402n` and
  `/tmp/onpc-e2e-evidence-39o02f_l`; attempt
  `scenario-598fadf79d6d4c479ea929b46b4060a0`. There was no VM cleanup to perform.
- `make check` during concurrent edits: 75 failed, 7,988 passed; every failure was in
  `tests/unit/test_codex_slices.py`. Concurrent edits added `REVIEW_PROMPT` to
  `tools/codex_slices.py::preflight`; existing synthetic checkouts then failed
  with `A required continuation document is missing`. Representative case:
  `test_each_fresh_session_uses_reassessed_model_and_effort[gpt-6-astra-high]`.
  The final aggregate stopped before components. These are new failures,
  separate from the original installer failures.

During this session, `tools/codex_slices.py` changed from no unstaged diff to
143 added and 2 removed lines, without this session editing it. This is direct
evidence of concurrent source activity during the rerun, but does not identify
the original run's changed field or writer. These unrelated edits were preserved.
The user confirmed that the other thread was still working, then reported it
finished and requested resumption. No further VM attempt was started until that
confirmation. The other thread's completed launcher changes and tests were
preserved.

## Settled-input rerun

- `tools/run-unit-tests 'tests/unit/test_codex_slices.py'
  'tests/unit/test_installer.py' 'tests/unit/test_e2e_provenance.py' -q`:
  253 passed. The transient launcher failures are resolved in the other thread's
  completed implementation.
- Final `make check`: passed, 8,081 unit tests in 132.33 seconds and
  134 private-D-Bus components in 9.80 seconds.
- Isolated cleanup prerequisites: 806 passed, 3 subtests passed in 8.34 seconds;
  the E2E launcher repeated them successfully in 9.03 seconds.
- `tools/run-tests artifacts build`: passed; fresh artifacts at
  `/tmp/onpc-test-artifacts-zazugd3l`.
- `tools/run-tests e2e --artifacts /tmp/onpc-test-artifacts-zazugd3l
  --scenario E2E-001`: passed, exit 0. Attempt
  `scenario-f54f8a8a7e2e49cdab2634cd588ac782` completed the GDM/serial/GDM-return
  observations, worker cleanup, baseline restoration and final input preservation.
  Product, infrastructure, collection and cleanup all passed; lease complete.
  Invocation evidence: `/tmp/onpc-e2e-evidence-9_tqkvjq`; acceptance:
  `/tmp/onpc-e2e-evidence-n0341lvb/acceptance.json`; raw artifacts:
  `/tmp/onpc-graphical-smoke-rydr5hsy`. Recorded stage durations: preparation
  374.879 seconds, test 551.092 seconds, cleanup 60.884 seconds. These stage
  figures are not total wall time; final preservation checks also took time.
- Accepted source digest:
  `5160aefa1a12948e52ebf7c806359b01c419f18d6cc5dd636df6e72b256d50bd`.
  Package digest:
  `3642671f6c281d7b09daf7990f7e6aee263db2f67d0c4ac29a7e0477f69ad0c5`.
  Full input identities are retained in the acceptance record. No checkout edits
  were made during the attempt. Only evidence/contract documentation was updated
  afterward; those updates invalidate artifact reuse under the current contract.
- `tools/test-vm status` after completion: state 5, id -1 (off).
  All commands launched by this investigation have finished. Markdown links and
  `git diff --check` pass.

All failed checks from the original report have now passed in relevant reruns.
The original source-change field and writer remain unproven: this successful
settled-input attempt does not close that historical attribution gap or prove
absence of intermittency. New rejection counts remain locally tested, without
a deliberately induced live failure. No further failed check remains in the
verified scope. The original report remains failed; no single current-input
`test-all` release acceptance is claimed.
