# 049 — E2E-015: child-overlay-cancel / child-overlay-escape

Budget: 30–50 minutes, including a normal verification cycle; this is not a stop timer.
Follow the [master session contract](../E2E-Execution-Plan.md#execute-one-task) and repository approvals.

## Scope and entry

Implement only these [inventory](../../../tests/e2e/scenarios.json) variants:

- **44 — child-overlay-cancel:** `surface=child-overlay`, `exit=cancel`.
- **45 — child-overlay-escape:** `surface=child-overlay`, `exit=escape`.

Use the master row's completed capabilities and only the recipe branches for the variants above. Resolve their callables from the catalogue/source; no earlier task document supplies context. Each variant starts a fresh guarded attempt with verified installed setup before customer steps.

## Work and visible acceptance

Compose the exact E2E-015 [recipe](../E2E-Building-Blocks.md#request-forms-and-remembered-choices) for these bindings in a thin Perl worker and `JourneyPlan`. Capture actual child activity, change overlay choices, exit separately by Cancel and Escape, then compare and use the same activity.

Bind inputs, public transitions and expectations before execution. Apply [inventory reconciliation](../E2E-Building-Blocks.md#inventory-reconciliation) to this consumer, preserving numeric IDs, sibling scope and displaced assertions' engineering owners.

## Live VM acceptance

Register the complete callable/variant using the [existing consumer path](../E2E-Building-Blocks.md#add-a-consumer), run affected checks, finish functional edits and execute:

Run these commands individually in order. Wait for successful cleanup before
each coverage refresh; stop on a failed run or refresh.

```sh
tools/run-tests e2e --id '44'
tools/generate_test_coverage.sh

tools/run-tests e2e --id '45'
tools/generate_test_coverage.sh
```

Every selected variant must pass its complete installed journey, all declared visible assertions and terminal collection/cleanup. Host tests, partial steps and another variant's result do not suffice. Preserve the master's ready-case regressions; failures leave this task unchecked.

## Close out

After each variant's full live journey and cleanup pass, the commands above
run `tools/generate_test_coverage.sh` (the approved launcher for
`tools/generate_test_coverage.py`), even if declarations did not change.
Generation must succeed; it does not prove execution. Repeat the refresh if
later close-out changes inventory declarations.
Then check this task in the [master](../E2E-Execution-Plan.md) and update the
completed scopes/status in [the catalogue](../E2E-Building-Blocks.md).
Delete this task when no longer needed, replacing its master link with plain
text. No new evidence/history document. Follow the master's Markdown checks.
