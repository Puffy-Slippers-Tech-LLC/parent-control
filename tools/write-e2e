#!/usr/bin/python3 -IB
"""Implement E2E tasks through fresh, reconnectable Codex sessions."""
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parent))
from write_e2e import main

if __name__ == '__main__':
    sys.exit(main())
