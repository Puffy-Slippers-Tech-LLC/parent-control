{
  "type": "object",
  "properties": {
    "status": {"type": "string", "enum": ["test_fixed", "fixed", "blocked", "app_issue", "uncertain"]},
    "summary": {"type": "string"}
  },
  "required": ["status", "summary"],
  "additionalProperties": false
}
