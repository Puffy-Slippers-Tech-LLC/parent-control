# 035 — Prepare one native app and operate it publicly

Budget: 40–60 minutes, including a normal verification cycle; this is not a stop timer.
Follow the [master session contract](../E2E-Execution-Plan.md#execute-one-task) and repository approvals.

## Scope and entry

Consumer: E2E-015 overlay exits (44, 45), then natural-expiry and native launch journeys. Scope: verified native asset preparation and APP01/02/03 native grid/command usable scope; policy-denial binding is qualified with FLOW03.

Required implemented capabilities: FILE01, FILE02, FILE06; UI16. Use maintained callables and an independent public entry state, never an earlier task document or attempt.

Contract: the named [catalogue rows](../E2E-Building-Blocks.md#ordered-building-block-catalogue) and consumer recipe. Qualify only the bindings named here.

## Work

Use supported asset preparation and FIX04 staging for a pinned native fixture, its finite matching/nonmatching and AppImage version examples, and a public usability action. These assets do not depend on desktop-launch support. Qualify APP01 grid/command attempt, APP02's usable observation and APP03 normal input plus visible effect in that order. Register SEARCH03/04/05 app projections; no hidden-launcher execution claim.

## Live VM acceptance

On the VM, launch the actual app from the grid and terminal and perform a normal action with visible result. Keep denial/hidden bindings pending for their public-policy consumer. No fake window or process probe.

Run affected safety/worker checks, then planned fixed qualification `tools/run-tests integration check_e2e_expiry`, or the full named consumer if runnable. Reuse/create the fixed entry under the master's qualification contract. Every result above and owned cleanup must pass on the live VM; diagnostic success earns no scenario coverage.

## Close out

After successful cleanup, check this task in the [master](../E2E-Execution-Plan.md) and update [catalogue/scenario status](../E2E-Building-Blocks.md) using its readiness rules. Delete this task when no longer needed, replacing its master link with plain text. No new evidence/history document.
